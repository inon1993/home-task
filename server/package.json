{
  "name": "home-task-server",
  "version": "1.0.0",
  "description": "My implementation for the home task.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client": "cd .. && cd client && npm start",
    "dev": "concurrently \"nodemon server.js\" \"npm run client\""
  },
  "author": "Inon Avramashvili",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.8.0"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
